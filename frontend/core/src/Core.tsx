import './Core.css';

const Core = () => {
  return (
    <div className="content">
      <h1>CORE MF</h1>
      <p>This is the "core" microfrontend. It houses shared layout components (e.g., Header, Footer) and reusable UI elements for use across the application.</p>
    </div>
  );
};

export default Core;
